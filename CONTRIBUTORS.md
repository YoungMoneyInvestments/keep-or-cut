# Contributors

- [YoungMoneyInvestments](https://github.com/YoungMoneyInvestments) — project maintainer.
- OpenAI Codex — public-release review, launch-film production, and contributor guidance.
- Cursor — CLI help/docs alignment for subscription model aliases (`cursor`, `grok`, `codex`, `gemini`).
- Grok — Grok CLI harness: pass bundle notes via `--system-prompt` so fair/system wrap modes stay valid.
- Claude — fixed a dashboard bug where a `--split skills` matrix with every leaf sharing a name prefix rendered zero rows (single-pass parent synthesis left orphaned ancestor chains).
