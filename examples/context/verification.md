# Verification habit (synthetic demo bundle)

Before returning an answer, check it against the task's stated constraints one more time
(word/line counts, required format, forbidden words). If your draft doesn't satisfy every
explicit constraint, fix it before responding — don't explain the constraint back to the user
instead of meeting it.
